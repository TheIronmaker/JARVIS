"""
Camera & UI Skeleton

Document-only skeleton. Implement `Camera` mirroring the C++ `Camera` struct:
- attributes: target, radius, azimuth, elevation, orbitSpeed, panSpeed
- methods: position(), process_mouse_move(x,y), process_scroll(dx,dy), process_mouse_button(...)

Keep camera pure (no GL calls) so it can be unit-tested.
"""

# class Camera:
#     pass
