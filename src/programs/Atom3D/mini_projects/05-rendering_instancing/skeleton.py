"""
Instancing Skeleton

Document-only file describing how to add instanced rendering.

Suggested sections:
- build_sphere_mesh(resolution)
- create_instance_buffer(positions, colors)
- draw_instanced(mesh, instance_buffer, count)

Notes:
- Keep sphere mesh small (low-poly) for many instances.
"""

# def build_sphere_mesh():
#     pass
