Instancing tests and notes
--------------------------

Manual checks:
- Instance attribute offsets and strides are correct.
- Colors match expected per-instance values.

Performance checks:
- Time to render 50k instances vs 100k instances; log frame time.
