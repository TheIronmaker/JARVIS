"""
Sampling & Special Functions - Skeleton

Document-only skeleton for implementing samplers using numpy+scipy.

Suggested functions:
- build_radial_cdf(n, l, rmax, Ngrid)
- sample_r_from_cdf(cdf, rgrid, u_array)
- sample_theta(l, m, Ngrid, u_array)
- sample_phi(N) -> uniform samples

Hints:
- Use `scipy.special.eval_genlaguerre` and `scipy.special.lpmv` for Laguerre/Legendre evaluation.
- Use `numpy.interp` for many u -> r samples from a precomputed CDF.
"""

# def build_radial_cdf(n,l,rmax,Ngrid):
#     pass
