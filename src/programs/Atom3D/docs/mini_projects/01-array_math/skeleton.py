"""
Array Math (NumPy) - Skeleton

This file intentionally contains no runnable implementation; use it as a place to implement
vectorized functions while following the README plan.

Suggested functions to implement:
- create_particle_arrays(N) -> returns dict of arrays for r,theta,phi,x,y,z
- spherical_to_cartesian(r,theta,phi) -> (x,y,z) vectorized
- cartesian_to_spherical(x,y,z) -> (r,theta,phi) vectorized
- update_phi(phi, dphi) -> new_phi (handle wrapping)

Notes:
- Use `numpy` for vector operations.
- Keep functions pure and unit-testable.
"""

# Example API surface (document only)
# def create_particle_arrays(N):
#     pass
