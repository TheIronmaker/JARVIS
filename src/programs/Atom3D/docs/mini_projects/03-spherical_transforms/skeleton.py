"""
Spherical Transforms - Skeleton

Document-only skeleton for implementing numerically stable transforms.

Suggested functions:
- cartesian_to_spherical(x,y,z) -> r,theta,phi
- spherical_to_cartesian(r,theta,phi) -> x,y,z
- safe_acos(x) that clamps input to [-1,1]

Edge cases to consider:
- r very small (near origin), theta near 0 or pi, phi wrapping.
"""

# def safe_acos(x):
#     pass
