[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jarvis"
version = "0.1.1"
description = "JARVIS modular control system"
authors = [{name = "Andy"}]
dependencies = [
    "PySide6",
]

[tool.setuptools.packages.find]
where = ["src"]
